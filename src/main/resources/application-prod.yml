spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ep-delicate-water-ahwvsfhl-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_3UeVGMk6lDEF

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    validate-on-migrate: true

server:
  port: 8082

logging:
  level:
    root: WARN
    lk.iit.campus: INFO

# Firebase Configuration for Production
# REQUIRED: Set environment variables for push notifications
firebase:
  enabled: ${FIREBASE_ENABLED:true}
  service-account:
    path: ${FIREBASE_SERVICE_ACCOUNT_PATH}
  token-cleanup:
    enabled: true
    days-old: 30
    cron: "0 0 3 * * *"

